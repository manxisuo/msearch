# MSearch Roadmap

面向 Linux（麒麟 ARM64）的 Everything 风格文件名快速检索工具。技术栈：Qt 5.12.8 + C++14 + CMake。

参考：[Everything](https://www.voidtools.com/)、[FSearch](https://github.com/cboxdoerfer/fsearch)

---

## 阶段总览

| 阶段 | 目标 | 状态 |
|------|------|------|
| 0 | 工程骨架 | 已完成 |
| 1 | MVP：索引 + 内存检索 + 基础 UI | 已完成 |
| 2 | 可用性增强 | 已完成 |
| 3 | Everything 体验（增量 / 常驻） | 已完成 |
| 4 | 高级检索能力 | 已完成 |
| 5 | 交付与麒麟 ARM64 | 基本完成（待实机确认） |

---

## 阶段 0 — 工程骨架 ✅

- [x] CMake 工程（Qt 5.12、C++14、AUTOMOC）
- [x] 目录划分：`app` / `index` / `search` / `model`
- [x] README、`.gitignore`

---

## 阶段 1 — MVP ✅

核心目标：指定目录建索引，边打字边搜，结果可打开。

- [x] `FileEntry` 条目模型（名称、路径、大小、修改时间、目录标记）
- [x] `IndexDatabase` 线程安全内存库 + 二进制持久化（`.msdb`）
- [x] `Indexer` 递归扫描、批量写入、可取消
- [x] 排除常见噪音目录（`.git`、`node_modules` 等）及 `/proc` `/sys` `/dev` `/run`
- [x] `SearchEngine` 后台线程子串匹配、查询令牌取消过期结果
- [x] 输入防抖、区分大小写、全部/仅文件/仅文件夹过滤
- [x] 结果表：名称 / 路径 / 大小 / 修改时间
- [x] 双击打开、右键打开所在目录、复制完整路径
- [x] 设置对话框：配置索引目录
- [x] 重建索引 / 取消索引
- [x] 启动加载已有索引，无则自动扫描（默认用户主目录）

---

## 阶段 2 — 可用性增强 ✅

让日常使用更顺手，接近 FSearch 基础能力。

- [x] 通配符搜索（`*` / `?`，整名匹配；无通配符时仍为子串）
- [x] 按名称、路径、大小、修改时间排序（点击表头）
- [x] 更多排除规则：路径/名称通配、隐藏文件开关、符号链接策略（防环）
- [x] 索引进度更细（当前目录 + 条数/秒）
- [x] 空查询提示 + 最近搜索历史（回车/打开时记录，补全弹出）
- [x] 结果数量上限可配置；超限时状态栏明确提示
- [x] 使用系统默认 Qt 样式（不强制自绘主题）
- [x] 基础快捷键：`Ctrl+L`/`Ctrl+F` 聚焦搜索，`Esc` 清空，`F5` 重建索引

---

## 阶段 3 — Everything 体验 ✅

补齐「常驻 + 文件一变就能搜」的体验差距。

- [x] **文件系统监控增量更新**（`QFileSystemWatcher`；Linux 底层为 inotify）
- [x] 启动异步加载索引；损坏则提示并自动重建
- [x] 系统托盘常驻、关闭/最小化可隐藏到托盘
- [x] 全局热键呼出主窗口（Linux X11 / Windows；默认 `Ctrl+Alt+Space`）
- [x] 开机自启（Linux `~/.config/autostart`；Windows 注册表 Run）
- [x] 多路径：移除即删子树，新增可增量扫描
- [x] 索引损坏检测（魔数/版本/截断标记）与自动重建

说明：监控目录数默认上限 8192，超出时依赖父目录变更触发刷新。Wayland 下全局热键可能不可用（需 X11）。

---

## 阶段 4 — 高级检索 ✅

对齐 Everything / FSearch 的进阶能力。

- [x] 正则表达式（`regex:` / `r:`，`QRegularExpression`）
- [x] 路径参与匹配（`path:` / `p:`）
- [x] 过滤器预设（`ext:` / `size:` / `dm:` + 工具栏快捷预设）
- [x] 书签（保存/选择常用查询）
- [x] **拼音首字母**检索（近似算法，可在设置中开关）
- [x] 多关键词 AND（空格）、OR（`a|b`）与排除（`-词`）
- [x] 搜索语法帮助面板（F1 / `?`）

---

## 阶段 5 — 交付与麒麟 ARM64 ✅（实机待验证）

可安装、可分发、在目标系统稳定运行。

- [x] aarch64 交叉编译工具链模板 + 实机编译文档（待目标机跑通勾选验证）
- [x] 麒麟 `.deb` 打包脚本（desktop 入口、SVG 图标、依赖声明）
- [x] 权限与挂载策略说明（默认家目录；全盘需自行授权）见 `docs/PACKAGING.md`
- [x] 跳过网络盘 / 只读盘的可配置策略
- [x] 基础自动化测试（`ctest`：查询解析、持久化往返、搜索匹配）
- [x] 性能基线记录表（文档参考指标）
- [x] 简体中文界面与文档（README / ROADMAP / PACKAGING）

---

## 明确不做 / 暂缓

| 项 | 说明 |
|----|------|
| 全文内容检索 | 定位是文件名检索，不做 Recoll 类全文 |
| 读 ext4 元数据捷径 | 脆弱、难维护；坚持扫描 + 监控 |
| Qt 6 / QML 重写 | 当前目标钉 Qt 5.12.8 Widgets |
| 非 Linux 优先适配 | 以麒麟 ARM64 为主；其他发行版后续顺带 |

---

## 建议推进顺序

1. ~~阶段 2 中的通配符 + 表头排序~~ ✅
2. ~~阶段 3 的 inotify 增量 + 托盘/热键~~ ✅
3. ~~阶段 4 高级检索~~ ✅
4. ~~阶段 5 打包/测试/挂载策略~~ ✅（aarch64 实机验证仍建议在麒麟板上执行一次）

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-28 | 初版：记录已完成 MVP，规划阶段 2–5 |
| 2026-07-28 | 完成阶段 2：通配符、排序、排除规则、历史、快捷键、结果上限 |
| 2026-07-28 | 完成阶段 3：监控增量、托盘、热键、自启、异步加载、部分重建 |
| 2026-07-28 | 完成阶段 4：查询语法、正则/路径/拼音、预设、书签、帮助 |
| 2026-07-28 | 完成阶段 5：deb 打包、挂载策略、单元测试、交叉编译/交付文档 |
